# 🔒 حل سريع: خطأ 403 عند تحديث البيانات

## المشكلة
عند محاولة تحديث عناصر التقييم، تظهر رسالة خطأ:
```
403 Forbidden - Not authenticated
```

---

## ⚡ الحل السريع (30 ثانية)

### ببساطة: **يجب تسجيل الدخول مرة أخرى!**

انتهت صلاحية جلستك. اتبع هذه الخطوات:

### الخطوة 1️⃣: حدّث الصفحة
اضغط **F5** أو زر التحديث في المتصفح

### الخطوة 2️⃣: انتظر رسالة التنبيه
ستظهر رسالة: **"غير مصرح بالوصول. يرجى تسجيل الدخول."**

### الخطوة 3️⃣: سيتم نقلك تلقائياً
سيتم نقلك لصفحة تسجيل الدخول تلقائياً

### الخطوة 4️⃣: سجل دخولك
استخدم بيانات تسجيل الدخول:
- **البريد:** testadmin@salamaty.com
- **كلمة المرور:** test123

### الخطوة 5️⃣: جرب التحديث مرة أخرى
الآن يمكنك تعديل وحفظ البيانات بنجاح! ✅

---

## 💡 طريقة بديلة (إذا لم تعمل الطريقة الأولى)

### افتح Console (اضغط F12)

### اكتب هذا الأمر واضغط Enter:
```javascript
localStorage.clear(); location.reload()
```

### سجل دخولك مرة أخرى

---

## ✅ تأكيد نجاح الحل

بعد تسجيل الدخول، يجب أن ترى:

✅ **لا توجد أخطاء 403 في console**  
✅ **تحميل البيانات بنجاح**  
✅ **إمكانية التعديل والحفظ**  
✅ **رسالة "تم التحديث بنجاح" عند الحفظ**

---

## 🔍 للتحقق من صلاحية الجلسة

افتح Console (F12) واكتب:

```javascript
console.log('Token:', localStorage.getItem('access_token') ? '✅ موجود' : '❌ غير موجود')
console.log('User:', localStorage.getItem('sallamaty_user') ? '✅ مسجل' : '❌ غير مسجل')
```

**النتيجة الصحيحة:**
```
Token: ✅ موجود
User: ✅ مسجل
```

---

## ⚠️ ملاحظات مهمة

### لماذا حدثت المشكلة؟
- انتهت **صلاحية الجلسة** (Token expired)
- الجلسات تنتهي بعد **30 دقيقة** من عدم النشاط
- هذا **إجراء أمني** لحماية حسابك

### كيف تتجنب المشكلة؟
1. ✅ **سجل الدخول** عند بدء العمل
2. ✅ **حدّث الصفحة** كل فترة أثناء العمل
3. ✅ **سجل الخروج** عند الانتهاء
4. ✅ **لا تترك الصفحة مفتوحة** لفترات طويلة بدون استخدام

---

## 🆘 ما زالت المشكلة موجودة؟

### تحقق من هذه النقاط:

#### 1. هل الخادم (Backend) يعمل؟
افتح terminal وتأكد:
```bash
cd backend
python -m uvicorn main:app --reload --port 8000
```

#### 2. هل بيانات الدخول صحيحة؟
جرب هذه البيانات:
- البريد: `testadmin@salamaty.com`
- كلمة المرور: `test123`

#### 3. هل قاعدة البيانات متصلة؟
تحقق من أن PostgreSQL يعمل

#### 4. جرب Incognito Mode
افتح نافذة خاصة (Incognito) وسجل دخولك

---

## 📞 المساعدة

إذا استمرت المشكلة:

1. افتح **Console (F12)**
2. التقط **screenshot** للأخطاء
3. افتح **Network tab** وتحقق من الطلبات
4. شارك **الأخطاء** مع فريق الدعم

---

## 📊 ملخص التحديثات

### ما تم إصلاحه:
✅ **معالجة خطأ 403** - رسالة واضحة وإعادة توجيه تلقائية  
✅ **تحديث تلقائي للـ token** - قبل كل طلب API  
✅ **رسائل واضحة** - للمستخدم عند انتهاء الصلاحية  
✅ **تسجيل مفصل** - في console لتسهيل التشخيص

### ما يجب فعله الآن:
1. **حدّث الصفحة** (F5)
2. **سجل دخولك** مرة أخرى
3. **ابدأ العمل** بشكل طبيعي

---

## ⏰ جدول صلاحية الجلسة

| الحالة | المدة | ماذا يحدث |
|-------|------|-----------|
| نشط | 0-30 دقيقة | ✅ كل شيء يعمل |
| تحذير | 25-30 دقيقة | ⚠️ قد تنتهي الجلسة قريباً |
| منتهي | 30+ دقيقة | ❌ يجب تسجيل الدخول مرة أخرى |

**نصيحة:** جدد الصفحة كل 15-20 دقيقة للحفاظ على الجلسة نشطة

---

**🎉 الآن يمكنك العمل بشكل طبيعي بعد تسجيل الدخول! 🎉**

