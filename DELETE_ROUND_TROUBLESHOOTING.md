# استكشاف أخطاء حذف الجولات

## المشكلة
عند محاولة حذف جولة، تظهر الرسالة:
```
حدث خطأ في حذف الجولة: Network error: Unable to connect to server. Please check if the server is running.
```

## الحلول المقترحة

### 1. التحقق من أن الباك اند شغال

#### طريقة 1: التحقق عبر Terminal
```bash
# تحقق من أن الباك اند شغال
curl http://localhost:8000/api/health

# أو
curl http://localhost:8000/docs
```

إذا لم تحصل على استجابة، قم بتشغيل الباك اند:
```bash
cd /Users/salehalzaid/Documents/massdeigners/salamah_rounds
./start_backend_macos.sh
```

#### طريقة 2: تحقق من المتصفح
افتح في المتصفح:
- http://localhost:8000/docs - لواجهة Swagger API
- تحقق من أن الصفحة تفتح بنجاح

### 2. التحقق من صلاحيات المستخدم

يجب أن يكون المستخدم الحالي أحد:
- `super_admin` (مدير النظام)
- `quality_manager` (مدير الجودة)

#### كيف تتحقق من صلاحياتك؟
1. افتح Console في المتصفح (F12)
2. اكتب:
```javascript
JSON.parse(localStorage.getItem('sallamaty_user')).role
```
3. تأكد أن النتيجة `super_admin` أو `quality_manager`

### 3. التحقق من التوكن

#### تحقق من وجود التوكن:
```javascript
// في Console المتصفح
console.log(localStorage.getItem('access_token'))
```

إذا كان التوكن موجود ولا يزال لديك مشكلة:
1. قم بتسجيل الخروج
2. سجل الدخول مرة أخرى
3. حاول الحذف مرة أخرى

### 4. التحقق من Console المتصفح

عند الضغط على "حذف":
1. افتح Developer Tools (F12)
2. اذهب إلى Console
3. ابحث عن رسائل الخطأ التفصيلية:
   - 🔗 API Request - يظهر URL والإعدادات
   - 📥 API Response - يظهر حالة الاستجابة
   - ❌ Error - يظهر الخطأ بالتفصيل

### 5. اختبار يدوي عبر curl

إذا كنت تريد اختبار الحذف يدويًا:

```bash
# احصل على التوكن من المتصفح
TOKEN="YOUR_TOKEN_HERE"

# استبدل 1 برقم الجولة المراد حذفها
ROUND_ID=1

# جرب الحذف
curl -X DELETE \
  "http://localhost:8000/api/rounds/${ROUND_ID}" \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json"
```

### 6. مشاكل شائعة وحلولها

#### المشكلة: "Could not validate credentials"
**الحل:**
- قم بتسجيل الخروج وتسجيل الدخول مرة أخرى
- تأكد من أن التوكن صحيح

#### المشكلة: "غير مصرح بالوصول" (403)
**الحل:**
- تأكد من أن دورك `super_admin` أو `quality_manager`
- إذا كنت `department_head` أو `assessor`، لا يمكنك حذف الجولات

#### المشكلة: "الجولة غير موجودة" (404)
**الحل:**
- تأكد من أن الجولة لم تُحذف مسبقًا
- قم بتحديث الصفحة (F5)

#### المشكلة: Network error
**الحل:**
- تأكد من أن الباك اند شغال على http://localhost:8000
- تحقق من أنه لا يوجد Firewall يمنع الاتصال
- تأكد من أن المنفذ 8000 ليس مستخدم من برنامج آخر

### 7. السجلات (Logs)

#### سجلات الباك اند
تحقق من ملف:
```bash
tail -f /Users/salehalzaid/Documents/massdeigners/salamah_rounds/backend/server.log
```

#### سجلات المتصفح
افتح Console وابحث عن:
- `🗑️ Attempting to delete round`
- `✅ Round deleted successfully`
- `❌ Error deleting round`

## التغييرات التي تمت

### 1. توسيع الصلاحيات
تم تعديل `src/components/pages/RoundsListView.tsx` للسماح لـ `quality_manager` بحذف الجولات (كان محصور على `super_admin` فقط).

### 2. تحسين رسائل الخطأ
تمت إضافة:
- رسالة نجاح عند الحذف
- سجلات console تفصيلية
- رسائل خطأ أوضح

## الكود المُحدَّث

```typescript
// قبل (يسمح فقط لـ super_admin)
if (!hasPermission(['super_admin'])) {
  alert('ليس لديك صلاحية لحذف الجولات. هذه الصلاحية مخصصة لمدير النظام فقط.')
  return
}

// بعد (يسمح لـ super_admin و quality_manager)
if (!hasPermission(['super_admin', 'quality_manager'])) {
  alert('ليس لديك صلاحية لحذف الجولات. هذه الصلاحية مخصصة لمدير النظام ومدير الجودة فقط.')
  return
}
```

## الاختبار

1. قم بتشغيل الباك اند
2. سجل الدخول كـ `super_admin` أو `quality_manager`
3. اذهب إلى صفحة الجولات (List View)
4. اضغط على زر "حذف" لأي جولة
5. أكد الحذف
6. يجب أن ترى رسالة "تم حذف الجولة بنجاح"

## إذا استمرت المشكلة

إذا جربت كل الحلول أعلاه ولا تزال المشكلة موجودة:

1. التقط screenshot من:
   - رسالة الخطأ
   - Console في المتصفح (F12)
   - Network tab في Developer Tools

2. شارك هذه المعلومات لمزيد من التشخيص

## ملاحظة مهمة

تأكد دائمًا من أن:
- ✅ الباك اند شغال على http://localhost:8000
- ✅ أنت مسجل دخول كمستخدم صحيح
- ✅ لديك الصلاحيات المناسبة (super_admin أو quality_manager)
- ✅ التوكن صحيح وغير منتهي

---
**تم التحديث:** 9 أكتوبر 2025

