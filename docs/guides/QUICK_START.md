# 🚀 دليل التشغيل السريع - نظام سلامتي

## المشكلة الحالية
- الخادم (Backend) لا يعمل
- لا توجد بيانات مستخدمين للدخول
- رسائل خطأ: "Network error" و "فشل في تسجيل الدخول"

## الحل السريع (5 دقائق)

### الخطوة 1: تشغيل قاعدة البيانات
```bash
# تأكد من تشغيل PostgreSQL
brew services start postgresql

# إنشاء قاعدة البيانات (إن لم تكن موجودة)
createdb salamaty_db
```

### الخطوة 2: تشغيل Backend
```bash
cd /Users/salehalzaid/Documents/massdeigners/salamah_rounds/backend

# إنشاء البيئة الافتراضية (إن لم تكن موجودة)
python -m venv venv

# تفعيل البيئة
source venv/bin/activate

# تثبيت المتطلبات
pip install -r requirements.txt

# نسخ ملف الإعدادات
cp env.local .env

# إنشاء الجداول والمستخدمين
python create_tables.py
python recreate_users.py

# تشغيل الخادم
python main.py
```

**المتوقع:** سترى رسالة:
```
INFO: Uvicorn running on http://0.0.0.0:8000
```

### الخطوة 3: تشغيل Frontend (ترمينال جديد)
```bash
cd /Users/salehalzaid/Documents/massdeigners/salamah_rounds

# تثبيت المتطلبات
npm install

# تشغيل الخادم
npm run dev
```

**المتوقع:** سترى:
```
Local: http://localhost:5174/
```

### الخطوة 4: اختبار تسجيل الدخول
افتح: http://localhost:5174

**بيانات الدخول:**
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `admin123`

أو:
- **الإيميل:** `admin@salamaty.com`  
- **كلمة المرور:** `admin123`

### الخطوة 5: مشاركة عبر Ngrok (ترمينال ثالث)
```bash
ngrok http 5174
```

انسخ الرابط وشاركه: `https://abc123.ngrok-free.app`

## بيانات المستخدمين المتاحة

| المستخدم | الإيميل | كلمة المرور | الدور |
|----------|---------|-------------|-------|
| admin | admin@salamaty.com | admin123 | مدير النظام |
| quality_manager | quality@salamaty.com | admin123 | مدير الجودة |
| ed_head | ed@salamaty.com | admin123 | رئيس قسم |
| assessor1 | assessor@salamaty.com | admin123 | مقيم جودة |
| viewer1 | viewer@salamaty.com | admin123 | مشاهد |

## استكشاف الأخطاء

### خطأ "Network error"
- تأكد من تشغيل Backend على http://localhost:8000
- اختبر: `curl http://localhost:8000/health`

### خطأ "فشل في تسجيل الدخول"
- تأكد من إنشاء المستخدمين: `python recreate_users.py`
- جرب بيانات مختلفة من الجدول أعلاه

### خطأ قاعدة البيانات
- تأكد من تشغيل PostgreSQL: `brew services start postgresql`
- تأكد من وجود قاعدة البيانات: `createdb salamaty_db`

## نصائح سريعة
- استخدم `admin` مع `admin123` للدخول الأول
- تأكد من تشغيل Backend قبل Frontend
- استخدم المنفذ 5174 للواجهة (ليس 3000)
- استخدم المنفذ 8000 للـ API
