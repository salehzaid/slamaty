# 🐛 تشخيص خطأ تحديث العنصر
## Debug Update Error

> الرسالة: **"حدث خطأ أثناء تحديث العنصر. يرجى المحاولة مرة أخرى."**

---

## 🔍 خطوات التشخيص السريعة

### الخطوة 1️⃣: افتح Developer Console

اضغط **F12** (أو Cmd+Option+J على Mac)

---

### الخطوة 2️⃣: انظر إلى Console

ابحث عن رسائل بهذا الشكل:

```javascript
❌ فشل في تحديث العنصر: Error: ...
❌ API request failed: ...
```

---

### الخطوة 3️⃣: افتح Network Tab

1. اذهب إلى تبويب **Network**
2. جرب التحديث مرة أخرى
3. ابحث عن طلب بلون **أحمر** (Failed)
4. اضغط عليه وانظر إلى:
   - **Status Code** (مثال: 403, 404, 500)
   - **Response** (الرسالة من الخادم)

---

## 🔧 الأخطاء الشائعة وحلولها

### الخطأ 1: 403 Forbidden
```
Error: HTTP error! status: 403, message: {"detail":"Not authenticated"}
```

**السبب:** انتهت صلاحية الجلسة

**الحل:**
```javascript
// في Console (F12)
localStorage.clear()
location.reload()
```
ثم سجل دخولك مرة أخرى

---

### الخطأ 2: 422 Unprocessable Entity
```
Error: HTTP error! status: 422, message: {"detail":"Validation error"}
```

**السبب:** البيانات المرسلة غير صحيحة

**الحل:**
1. تأكد من ملء **جميع الحقول المطلوبة** (*):
   - العنوان (عربي) *
   - التصنيف *
   - ارتباط العنصر *
   - نوع الدليل *
   - مستوى الخطر *
   - التوجيه (عربي) *

---

### الخطأ 3: 404 Not Found
```
Error: HTTP error! status: 404
```

**السبب:** العنصر لا يوجد في قاعدة البيانات

**الحل:**
1. حدّث القائمة
2. تأكد أن العنصر موجود
3. جرب إنشاء عنصر جديد بدلاً من التحديث

---

### الخطأ 4: 500 Internal Server Error
```
Error: HTTP error! status: 500
```

**السبب:** خطأ في الخادم (Backend)

**الحل:**
1. تحقق من أن Backend يعمل:
   ```bash
   cd backend
   python -m uvicorn main:app --reload --port 8000
   ```
2. تحقق من logs:
   ```bash
   tail -f backend/server.log
   ```

---

### الخطأ 5: Network Error
```
Error: Failed to fetch
Error: Network error: Unable to connect to server
```

**السبب:** الخادم لا يعمل أو مشكلة في الاتصال

**الحل:**
1. تأكد من تشغيل Backend على port 8000
2. تحقق من URL في المتصفح: `http://localhost:8000`
3. تأكد من عدم وجود Firewall يمنع الاتصال

---

## 📋 معلومات مطلوبة للمساعدة

إذا استمرت المشكلة، أرجو مشاركة:

### 1. من Console Tab:
```javascript
// انسخ الأخطاء الحمراء التي تراها
❌ فشل في تحديث العنصر: ...
❌ API request failed: ...
```

### 2. من Network Tab:
```
Status Code: ???
Response: {...}
Request URL: http://localhost:8000/api/...
```

### 3. البيانات التي تحاول حفظها:
```
عنوان العنصر: ???
نوع الدليل: ???
الخ...
```

---

## 🧪 اختبار التشخيص

### اختبار 1: هل الخادم يعمل؟

افتح في المتصفح:
```
http://localhost:8000/api/health
```

**النتيجة المتوقعة:**
```json
{"status": "healthy"}
```

---

### اختبار 2: هل أنت مسجل دخول؟

في Console (F12):
```javascript
console.log('Token:', localStorage.getItem('access_token') ? '✅ موجود' : '❌ غير موجود')
console.log('User:', localStorage.getItem('sallamaty_user') ? '✅ مسجل' : '❌ غير مسجل')
```

**النتيجة المتوقعة:**
```
Token: ✅ موجود
User: ✅ مسجل
```

---

### اختبار 3: البيانات في Console

قبل الضغط على "تحديث"، افتح Console وستجد:
```javascript
بيانات النموذج المجمعة: {
  title: "...",
  evidence_type: "...",
  // الخ
}

نوع الدليل المجمع: "OBSERVATION,DOCUMENT"

تحديث العنصر مع البيانات: {
  // البيانات الكاملة
}
```

إذا رأيت هذه الرسائل، فالمشكلة في **API/Backend**

إذا لم تراها، فالمشكلة في **Frontend**

---

## 🔍 تحليل تفصيلي

### سيناريو 1: الخطأ يحدث فوراً (قبل إرسال الطلب)

**السبب المحتمل:** خطأ في JavaScript

**التحقق:**
```javascript
// في Console - ابحث عن
Uncaught TypeError: ...
Uncaught ReferenceError: ...
```

**الحل:** أخبرني بالخطأ الدقيق

---

### سيناريو 2: الخطأ يحدث بعد ثانية أو اثنتين (بعد إرسال الطلب)

**السبب المحتمل:** خطأ من API/Backend

**التحقق:**
- افتح Network Tab
- ابحث عن طلب API بلون أحمر
- اقرأ Response

**الحل:** أخبرني بـ Status Code والـ Response

---

### سيناريو 3: الخطأ يحدث بعد وقت طويل (timeout)

**السبب المحتمل:** الخادم بطيء أو معلق

**التحقق:**
```bash
# تحقق من Backend logs
cd backend
tail -f server.log
```

**الحل:** أعد تشغيل Backend

---

## 🚑 حلول سريعة (جربها بالترتيب)

### الحل السريع 1: تحديث قسري للصفحة
```
اضغط: Ctrl + Shift + R (أو Cmd + Shift + R على Mac)
```

### الحل السريع 2: مسح البيانات المحلية
```javascript
// في Console (F12)
localStorage.clear()
location.reload()
```

### الحل السريع 3: إعادة تسجيل الدخول
1. سجل الخروج
2. أغلق المتصفح
3. افتحه من جديد
4. سجل الدخول

### الحل السريع 4: Incognito Mode
1. افتح نافذة خاصة (Incognito/Private)
2. سجل الدخول
3. جرب التحديث

### الحل السريع 5: إعادة تشغيل Backend
```bash
# أوقف Backend (Ctrl+C)
# ثم شغله من جديد
cd backend
python -m uvicorn main:app --reload --port 8000
```

---

## 📞 إذا لم ينجح شيء...

أرجو مشاركة:

1. **Screenshot من Console** (الأخطاء الحمراء)
2. **Screenshot من Network Tab** (الطلب الفاشل)
3. **نسخ من البيانات** التي تحاول حفظها
4. **نسخ من logs** إذا أمكن

---

## 💡 نصائح للوقاية

1. ✅ **اختبر في Development Environment أولاً**
2. ✅ **احتفظ بـ Console مفتوح** أثناء العمل
3. ✅ **تحقق من البيانات** قبل الحفظ
4. ✅ **سجل الخروج والدخول** كل فترة
5. ✅ **راجع التوثيق** عند الشك

---

**🔧 جاهز للمساعدة! فقط أخبرني بما تراه في Console 🔧**

