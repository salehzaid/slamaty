# 🚀 دليل النشر على Railway - بالتفصيل الممل

## 📋 ما هو Railway؟
Railway هو منصة نشر سحابية تدعم:
- ✅ Docker containers
- ✅ قواعد بيانات PostgreSQL مجانية
- ✅ نشر تلقائي من GitHub
- ✅ SSL certificates مجانية
- ✅ 500 ساعة تشغيل مجانية شهرياً

## 🎯 النتيجة النهائية
- رابط ثابت مثل: `https://salamah-rounds-production.up.railway.app`
- يعمل 24/7
- SSL مجاني
- قاعدة بيانات PostgreSQL

---

## الخطوة 1: إعداد GitHub (إجباري)

### أ) إنشاء repository على GitHub
1. اذهب إلى: https://github.com/new
2. **Repository name**: `salamah-rounds`
3. **Description**: `نظام سلامتي - إدارة جولات الجودة`
4. اختر **Public** (مجاني) أو **Private** (إذا كان لديك خطة)
5. اضغط **Create repository**

### ب) ربط المشروع المحلي بـ GitHub
```bash
# من مجلد المشروع
cd /Users/salehalzaid/Documents/massdeigners/salamah_rounds

# تهيئة git (إن لم يكن مفعلاً)
git init

# إضافة جميع الملفات
git add .

# إنشاء commit
git commit -m "Initial commit for Railway deployment"

# ربط بـ GitHub (استبدل YOUR_USERNAME باسم المستخدم)
git remote add origin https://github.com/YOUR_USERNAME/salamah-rounds.git

# رفع الكود
git branch -M main
git push -u origin main
```

**ما تتوقعه:** سترى الملفات تُرفع إلى GitHub

---

## الخطوة 2: إنشاء حساب Railway

### أ) التسجيل
1. اذهب إلى: https://railway.app
2. اضغط **Sign Up**
3. اختر **Sign up with GitHub** (الأسهل)
4. أذن لـ Railway بالوصول لحساب GitHub

### ب) التحقق من الحساب
- ستحصل على 500 ساعة مجانية شهرياً
- لا تحتاج بطاقة ائتمان للبدء

---

## الخطوة 3: إنشاء مشروع على Railway

### أ) إنشاء مشروع جديد
1. في لوحة Railway، اضغط **New Project**
2. اختر **Deploy from GitHub repo**
3. اختر repository `salamah-rounds`
4. اضغط **Deploy Now**

### ب) ما سيحدث
- Railway سيكتشف أن المشروع يحتوي Docker files
- سيبدأ في بناء الصور تلقائياً
- قد يستغرق 2-5 دقائق

---

## الخطوة 4: إضافة قاعدة البيانات PostgreSQL

### أ) إضافة PostgreSQL
1. في مشروعك على Railway، اضغط **+ New**
2. اختر **Database**
3. اختر **Add PostgreSQL**
4. انتظر حتى يكتمل الإعداد (30-60 ثانية)

### ب) الحصول على رابط قاعدة البيانات
1. اضغط على **PostgreSQL** في مشروعك
2. اذهب إلى تاب **Connect**
3. انسخ **Database URL** (يبدو مثل):
```
postgresql://postgres:password@roundhouse.proxy.rlwy.net:12345/railway
```

---

## الخطوة 5: ضبط متغيرات البيئة

### أ) الوصول لإعدادات المشروع
1. اضغط على **salamah-rounds** (الخدمة الرئيسية)
2. اذهب إلى تاب **Variables**
3. اضغط **+ New Variable**

### ب) إضافة المتغيرات المطلوبة

**أضف هذه المتغيرات واحداً تلو الآخر:**

1. **DATABASE_URL**
   - Name: `DATABASE_URL`
   - Value: `${{Postgres.DATABASE_URL}}` (Railway سيملأها تلقائياً)

2. **SECRET_KEY**
   - Name: `SECRET_KEY`
   - Value: `railway-secret-key-production-2024`

3. **CORS_ORIGINS**
   - Name: `CORS_ORIGINS`
   - Value: `*` (أو رابط الواجهة المحدد)

4. **ENVIRONMENT**
   - Name: `ENVIRONMENT`
   - Value: `production`

5. **PORT**
   - Name: `PORT`
   - Value: `8000`

### ج) حفظ المتغيرات
اضغط **Add** لكل متغير

---

## الخطوة 6: ضبط إعدادات النشر

### أ) إعدادات البناء
1. في تاب **Settings**
2. تحت **Build**:
   - **Builder**: Docker
   - **Dockerfile Path**: `Dockerfile.backend`
   - **Root Directory**: `/` (افتراضي)

### ب) إعدادات التشغيل
1. تحت **Deploy**:
   - **Start Command**: `python main.py`
   - **Health Check Path**: `/health`

---

## الخطوة 7: إعادة النشر

### أ) إعادة البناء
1. اذهب إلى تاب **Deployments**
2. اضغط **Deploy Latest**
3. انتظر اكتمال البناء (2-5 دقائق)

### ب) مراقبة السجلات
1. في تاب **Logs** ستشاهد:
```
INFO: Started server process
INFO: Uvicorn running on http://0.0.0.0:8000
```

---

## الخطوة 8: الحصول على الرابط العام

### أ) رابط الخدمة
1. في لوحة المشروع، ستجد رابط مثل:
```
https://salamah-rounds-production.up.railway.app
```

### ب) اختبار الرابط
- افتح الرابط في المتصفح
- يجب أن ترى واجهة النظام
- جرب تسجيل الدخول بـ: `testuser` / `test123`

---

## الخطوة 9: إعداد الواجهة (Frontend)

### أ) إنشاء خدمة منفصلة للواجهة
1. في نفس المشروع، اضغط **+ New**
2. اختر **GitHub Repo**
3. اختر نفس repository
4. في **Settings**:
   - **Root Directory**: `/`
   - **Build Command**: `npm run build`
   - **Start Command**: `npx serve dist -p $PORT`

### ب) متغيرات بيئة الواجهة
- **VITE_API_URL**: `https://your-backend-url.railway.app`

---

## الخطوة 10: الاختبار النهائي

### أ) اختبار Backend
```bash
curl https://your-backend-url.railway.app/health
# المتوقع: {"status":"healthy"}
```

### ب) اختبار تسجيل الدخول
```bash
curl -X POST "https://your-backend-url.railway.app/auth/signin" \
-H "Content-Type: application/json" \
-d '{"username": "testuser", "password": "test123"}'
# المتوقع: access_token
```

---

## 🔧 استكشاف الأخطاء

### خطأ "Build Failed"
- تحقق من **Logs** في تاب Deployments
- تأكد من وجود `Dockerfile.backend`
- تأكد من صحة `requirements.txt`

### خطأ "Database Connection"
- تحقق من متغير `DATABASE_URL`
- تأكد من أن PostgreSQL service يعمل
- راجع سجلات قاعدة البيانات

### خطأ "Service Unavailable"
- تحقق من أن PORT=8000
- تأكد من `python main.py` في Start Command
- راجع Application Logs

---

## 📱 النتيجة النهائية

### للمشاركة مع الزملاء:
```
🔗 نظام سلامتي: https://salamah-rounds-production.up.railway.app

🔑 بيانات الدخول:
👤 اسم المستخدم: testuser
🔑 كلمة المرور: test123

📋 النظام: إدارة جولات الجودة وسلامة المرضى
✅ متاح 24/7
🔒 SSL آمن
```

---

## ⏱️ الجدول الزمني المتوقع

| الخطوة | الوقت |
|--------|------|
| إعداد GitHub | 2 دقيقة |
| إنشاء حساب Railway | 1 دقيقة |
| ربط المشروع | 1 دقيقة |
| إضافة PostgreSQL | 1 دقيقة |
| ضبط المتغيرات | 2 دقيقة |
| النشر والاختبار | 3 دقائق |
| **المجموع** | **10 دقائق** |

---

## 🎯 هل تريد البدء؟

**أخبرني إذا كنت جاهزاً وسأرشدك خطوة بخطوة بدءاً من رفع الكود إلى GitHub!**
