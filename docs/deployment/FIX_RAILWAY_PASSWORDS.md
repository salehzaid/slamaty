# إصلاح كلمات المرور في Railway

## المشكلة
فشل تسجيل الدخول في بيئة الإنتاج (Railway) بسبب تشفير غير صحيح لكلمات المرور.

## الحل

### الخطوة 1️⃣: الحصول على DATABASE_URL من Railway

1. افتح [Railway Dashboard](https://railway.app)
2. سجل الدخول إلى حسابك
3. اذهب إلى مشروع `qualityrounds-production`
4. افتح قسم **Variables** أو **Settings**
5. ابحث عن `DATABASE_URL`
6. انسخ القيمة الكاملة (يجب أن تكون على هذا الشكل):
   ```
   postgresql://username:password@host/database
   ```

### الخطوة 2️⃣: تشغيل سكريبت الإصلاح محلياً

افتح Terminal وقم بتنفيذ الأوامر التالية:

```bash
# انتقل إلى مجلد المشروع
cd /Users/salehalzaid/Documents/massdeigners/salamah_rounds

# فعّل البيئة الافتراضية
source .venv/bin/activate

# عيّن DATABASE_URL (استبدل بالقيمة من Railway)
export DATABASE_URL="postgresql://neondb_owner:YOUR_PASSWORD@ep-lingering-morning-a0dejreab.us-east-1.aws.neon.tech/neondb?sslmode=require"

# شغّل سكريبت الإصلاح
python3 backend/railway_fix_passwords.py
```

### الخطوة 3️⃣: نشر التحديثات إلى Railway

بعد التأكد من أن السكريبت نجح، قم بنشر آخر تحديثات الكود:

```bash
# إضافة التغييرات
git add backend/auth.py

# عمل commit
git commit -m "Fix bcrypt password hashing issue"

# نشر إلى Railway
git push origin main
```

### الخطوة 4️⃣: اختبار تسجيل الدخول

بعد تشغيل السكريبت، اذهب إلى:
- 🌐 https://qualityrounds-production.up.railway.app/
- 📧 البريد الإلكتروني: `admin@salamaty.com`
- 🔑 كلمة المرور: `123456`

## المستخدمون المُحدَّثون

| البريد الإلكتروني | كلمة المرور | الدور |
|-------------------|-------------|--------|
| admin@salamaty.com | 123456 | مدير النظام |
| quality@salamaty.com | 123456 | مدير الجودة |
| ed@salamaty.com | 123456 | رئيس القسم |
| assessor@salamaty.com | 123456 | مقيّم |
| viewer@salamaty.com | 123456 | مشاهد |

## ملاحظات أمنية ⚠️

بعد تسجيل الدخول لأول مرة، يُنصح بشدة بتغيير كلمة المرور إلى كلمة مرور قوية!

## إذا واجهت مشاكل

### مشكلة: لا يمكن الاتصال بقاعدة البيانات
تأكد من:
- صحة DATABASE_URL
- وجود كلمة المرور في الرابط
- اتصالك بالإنترنت

### مشكلة: bcrypt error
تأكد من تثبيت bcrypt 4.1.3:
```bash
pip install bcrypt==4.1.3
```

## البديل: تشغيل السكريبت على Railway مباشرة

يمكنك أيضاً تشغيل السكريبت مباشرة على خادم Railway:

1. اذهب إلى Railway Dashboard
2. افتح Terminal في مشروعك
3. شغّل: `python backend/railway_fix_passwords.py`

