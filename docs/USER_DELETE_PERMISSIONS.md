# صلاحيات حذف المستخدمين - مدير النظام

## نظرة عامة
تم إضافة صلاحيات حذف المستخدمين حصرياً لمدير النظام (super_admin) لضمان الأمان والسيطرة على إدارة المستخدمين في النظام.

## الميزات المضافة

### 1. الواجهة الأمامية (Frontend)
- **تقييد واجهة الحذف**: زر الحذف يظهر فقط لمدير النظام
- **رسائل توضيحية**: إشعارات واضحة حول الصلاحيات المتاحة
- **تأكيد محسن**: نافذة تأكيد مفصلة مع معلومات المستخدم المراد حذفه
- **حماية من الحذف الذاتي**: منع مدير النظام من حذف حسابه الخاص

### 2. الخادم الخلفي (Backend)
- **فحص الصلاحيات**: التحقق من دور المستخدم قبل السماح بالحذف
- **حماية إضافية**: منع حذف مديري النظام الآخرين
- **رسائل خطأ واضحة**: رسائل مفصلة باللغة العربية
- **تسجيل العمليات**: تتبع عمليات الحذف في سجل النظام

## قواعد الأمان

### المستخدمون الذين يمكن حذفهم:
- ✅ المستخدمون العاديون (viewer, assessor, department_head, quality_manager)
- ❌ مديرو النظام الآخرين (super_admin)
- ❌ المستخدم نفسه (منع الحذف الذاتي)

### المستخدمون الذين يمكنهم الحذف:
- ✅ مدير النظام فقط (super_admin)
- ❌ جميع الأدوار الأخرى

## الاستخدام

### للمدير العام:
1. انتقل إلى صفحة "إدارة المستخدمين"
2. ستظهر رسالة تحذيرية حمراء تؤكد صلاحياتك
3. اضغط على زر الحذف (🗑️) بجانب المستخدم المراد حذفه
4. أكد العملية في النافذة المنبثقة
5. سيتم حذف المستخدم نهائياً

### للمستخدمين الآخرين:
1. انتقل إلى صفحة "إدارة المستخدمين"
2. ستظهر رسالة زرقاء توضح أن الحذف متاح للمدير فقط
3. لن تظهر أزرار الحذف في واجهة المستخدم

## رسائل النظام

### رسائل النجاح:
- `تم حذف المستخدم [الاسم] بنجاح`

### رسائل الخطأ:
- `ليس لديك صلاحية لحذف المستخدمين. هذه الصلاحية متاحة لمدير النظام فقط.`
- `لا يمكن حذف حسابك الخاص`
- `لا يمكن حذف مدير نظام آخر. يمكن فقط لمدير النظام حذف المستخدمين العاديين.`
- `المستخدم غير موجود`

## الأمان والخصوصية

- **تشفير البيانات**: جميع البيانات محمية بتشفير قوي
- **تسجيل العمليات**: كل عملية حذف مسجلة في سجل النظام
- **التحقق المزدوج**: فحص الصلاحيات في الواجهة والخادم
- **حماية من التلاعب**: منع التلاعب بالطلبات من جانب العميل

## التطوير المستقبلي

- [ ] إضافة سجل مفصل لعمليات الحذف
- [ ] إمكانية استرداد المستخدمين المحذوفين
- [ ] إشعارات للمديرين عند حذف المستخدمين
- [ ] تصدير سجل عمليات الحذف

## الملفات المعدلة

### الواجهة الأمامية:
- `src/components/pages/UsersManagement.tsx`

### الخادم الخلفي:
- `backend/main.py`

## الاختبار

لاختبار الوظيفة:
1. سجل دخول كمدير نظام
2. انتقل إلى إدارة المستخدمين
3. جرب حذف مستخدم عادي
4. تأكد من عدم إمكانية حذف مدير نظام آخر
5. تأكد من عدم إمكانية حذف نفسك

## الدعم الفني

في حالة وجود مشاكل:
1. تأكد من تسجيل الدخول كمدير نظام
2. تحقق من اتصال الخادم
3. راجع سجل الأخطاء في وحدة التحكم
4. تواصل مع فريق التطوير
